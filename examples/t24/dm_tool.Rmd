---
title: "DM Tool"
output:
  html_document: default
  word_document: default
---
```{r echo=FALSE}
# Load analysis functions (TODO: still not happy with the name... sba?)
source('../../wda.R')
source('../../obs.R')

suppressWarnings(library(readxl))
```


```{r}
# Load observations
dmo <- read_excel('../../data/tdm.xlsx')

# Initiate the analysis sequence
dm <- analysis('Temenos Data Migration Tool', 'dmo')

```

# Preparational structure

*TODO: Add directories and files*

```{r fig.width=15, fig.height=10}
(dm <- grow(dm, 'DM.PREREQUISITE', width = 2) %>%
   grow('DM.PREREQUISITE', width = 0, depth = 1))
```

# Observed structure

*TODO: Adding values crashes*

```{r  fig.width=15, fig.height=10}
(dm <- grow(dm, 'DM.PREREQUISITE', width = 2, depth = 1))
```

## Conclusions

What can we conclude based on analysis (or in word only?)

```{r fig.width=15, fig.height=10}

```

