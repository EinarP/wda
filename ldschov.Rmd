---
title: "Generation"
output: word_document
---
```{r message=FALSE}
#Common functions
source('func.R')

#Load observations file
obs <- read.csv('../data/obs.csv', stringsAsFactors=FALSE)

#Create analysis sequence (perhaps a tree later)

ldsch <- asq('LD scheduling overview')
ldsch <- addCenter(ldsch, 'LD.LOANS.AND.DEPOSITS')
ldsch

vc <- c(c1, c2, c...)
ldsch <- voidCenter(ldscd, vc)

addCenter(ldsch, 'LD.LOANS.AND.DEPOSITS', scan='LD')

scaleDown(ldsch)
scaleUp(ldsch)

ldsch

```
adjm <- matrix(c(0, 9, 0, 0), nrow=2, byrow=TRUE)
colnames(adjm) <- c('LD', 'CUS')
rownames(adjm) <- c('LD', 'CUS')

adjm <- matrix(sample(0:1, 100, replace=TRUE, prob=c(0.9,0.1)), nc=10)
adjm

g1 <- graph.adjacency(adjm)
g1

plot(g1)
